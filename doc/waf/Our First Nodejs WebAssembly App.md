# Our First Node.js WebAssembly App

We’ll begin the book by using [[Node.js]] as the embedding environment instead of the web browser so as to remove the need for HTML and CSS

![[waf/func#param]]
![[wat2wasm]]
