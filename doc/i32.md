# i32
![[waf/types|types]]

## add
stack: `( a:i32 b:i32 -- a+b:i32 )`
sexpr: `(i32.add i32 i32) -> i32`

- [[waf/i32#add|i32.add]]: typed `add`ing operation
	- takes two 32-bit integer variables off the stack,
	- adds them together, and then
	- pushes the result back onto the top
