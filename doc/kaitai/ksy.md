# .ksy
## [[kaitai]] format specification

Первый блок “**meta**” вроде понятен сам по себе - набор обязательный полей, дающий понять структура какого именно файла описана ниже.

```yaml
meta:
    id: wasm
    file-extension: wasm
    application: WebAssembly bytecode
    endian: le
```
